#include<cs50.h>
#include<stdio.h>
int fact(int n);
int main(void)
{
    int x= get_int("Enter a number: ");
    printf("Answer=%d\n",fact(x));
}
int fact(int n)
{
    if(n==1)
     return 1;
    else
     return n*fact(n-1);
}
